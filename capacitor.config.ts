import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.iahgames.kaalchakra",
  appName: "ChitraHarsha",
  webDir: "www",
  server: {
    androidScheme: "https",
  },
};

export default config;
